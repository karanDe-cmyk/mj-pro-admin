apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: matka  # ✅ PVC me namespace hona chahiye
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
storageClassName: mongodb-storage  # ✅ Same as PV
