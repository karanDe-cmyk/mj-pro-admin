apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matka-ingress
  namespace: matka
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
  - hosts:
      - www.dhanlaxmionlinematka.com
      - dhanlaxmionlinematka.com
      - kalyandpboss.shop
      - www.kalyandpboss.shop
      - api.kalyandpboss.shop
      - admin.kalyandpboss.shop
    secretName: matka-tls-secret
  rules:
  - host: dhanlaxmionlinematka.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 3000
  - host: www.dhanlaxmionlinematka.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 3000
  - host: kalyandpboss.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 3000
  - host: www.kalyandpboss.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 3000
  - host: api.kalyandpboss.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 5000
  - host: admin.kalyandpboss.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-admin-service
            port:
              number: 3000
